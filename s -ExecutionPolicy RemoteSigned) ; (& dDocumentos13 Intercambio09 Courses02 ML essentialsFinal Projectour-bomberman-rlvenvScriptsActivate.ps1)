[1mdiff --git a/agent_code/taco_kebab_agent/model.py b/agent_code/taco_kebab_agent/model.py[m
[1mindex 56f1df7..6de952a 100644[m
[1m--- a/agent_code/taco_kebab_agent/model.py[m
[1m+++ b/agent_code/taco_kebab_agent/model.py[m
[36m@@ -4,8 +4,11 @@[m [mOne linear function per action, Q(s, a) = beta_a . phi(s) + bias_a, fitted by[m
 ridge-regularised least squares on n-step TD targets. The bias is folded into[m
 beta_a by augmenting phi(s) with a constant 1.0 feature.[m
 [m
[31m-Model B (fitted Q-iteration with a regression forest) will share the same[m
[31m-`predict_q` / `update` / `save` / `load` interface in a future file.[m
[32m+[m[32mModel B (fitted Q-iteration with a regression forest) shares the same[m
[32m+[m[32m`predict_q` / `refit` / `save` / `load` interface, in `model_b.py`. Its[m
[32m+[m[32m`update()` deliberately raises NotImplementedError -- a forest has no[m
[32m+[m[32mincremental fit, so `refit(batch)` is the entry point train.py uses for both[m
[32m+[m[32mmodels; see interface_contract.md section 4.[m
 """[m
 [m
 import os[m
[36m@@ -42,12 +45,24 @@[m [mclass Model:[m
     """[m
 [m
     def __init__(self, ridge_lambda: float = 1.0, n_features: int = FEATURE_DIM,[m
[31m-                 n_step: int = 1, gamma: float = 0.99):[m
[32m+[m[32m                 n_step: int = 1, gamma: float = 0.99, forget: float = 1.0):[m
         self.ridge_lambda = ridge_lambda[m
         self.n_features = n_features[m
         self.n_step = n_step[m
         self.gamma = gamma[m
 [m
[32m+[m[32m        # Discount applied to the accumulated statistics before each batch is[m
[32m+[m[32m        # folded in. forget = 1.0 keeps every transition for ever, which is[m
[32m+[m[32m        # correct for a fixed regression target but wrong for ours: the target[m
[32m+[m[32m        # is reward + gamma * max_q(next), computed with whatever beta was at[m
[32m+[m[32m        # the time, so a target generated in round 1 from a zero Q-function[m
[32m+[m[32m        # carries the same weight as one from round 4000. Measured effect:[m
[32m+[m[32m        # ||A_BOMB|| reached 43900 over a 4000-round curriculum, so the last[m
[32m+[m[32m        # stage -- the one that has to transfer to the tournament -- had the[m
[32m+[m[32m        # least capacity left to learn. forget < 1 gives an effective window of[m
[32m+[m[32m        # roughly 1 / (1 - forget) update calls.[m
[32m+[m[32m        self.forget = forget[m
[32m+[m
         self.actions = ACTIONS[m
         dim = n_features + 1  # +1 for the bias term[m
 [m
[36m@@ -56,6 +71,10 @@[m [mclass Model:[m
         # actions accumulate transitions at different rates under epsilon-greedy.[m
         penalty = ridge_lambda * np.eye(dim, dtype=np.float64)[m
         penalty[-1, -1] = 0.0[m
[32m+[m[32m        #: Kept so the ridge term can be topped back up after each decay --[m
[32m+[m[32m        #: without that it would decay away too and the fit lose its[m
[32m+[m[32m        #: regularisation exactly when the data is sparsest.[m
[32m+[m[32m        self._penalty = penalty[m
         self._A = {action: penalty.copy() for action in self.actions}[m
         self._b = {action: np.zeros(dim, dtype=np.float64) for action in self.actions}[m
         self._beta = {action: np.zeros(dim, dtype=np.float64) for action in self.actions}[m
[36m@@ -72,6 +91,19 @@[m [mclass Model:[m
         q_values = np.array([phi @ self._beta[action] for action in self.actions], dtype=np.float32)[m
         return q_values[m
 [m
[32m+[m[32m    def predict_q_batch(self, features: np.ndarray) -> np.ndarray:[m
[32m+[m[32m        """Q-values for many states at once: (n, n_features) -> (n, n_actions).[m
[32m+[m
[32m+[m[32m        Same arithmetic as predict_q, but the replay refit needs the maximum[m
[32m+[m[32m        over actions for every transition in the buffer on every refit, and[m
[32m+[m[32m        doing that one state at a time in Python is what would make refitting[m
[32m+[m[32m        too slow to use.[m
[32m+[m[32m        """[m
[32m+[m[32m        phi = np.hstack([np.asarray(features, dtype=np.float64),[m
[32m+[m[32m                         np.ones((len(features), 1))])[m
[32m+[m[32m        beta = np.stack([self._beta[action] for action in self.actions], axis=1)[m
[32m+[m[32m        return phi @ beta[m
[32m+[m
     def update(self, batch: list[Transition]) -> None:[m
         by_action = {action: [] for action in self.actions}[m
         for transition in batch:[m
[36m@@ -84,10 +116,38 @@[m [mclass Model:[m
             phi = np.stack([self._augment(t.features) for t in transitions])[m
             y = np.array([t.reward for t in transitions], dtype=np.float64)[m
 [m
[32m+[m[32m            if self.forget < 1.0:[m
[32m+[m[32m                # Decay what we already know, then restore the ridge term so it[m
[32m+[m[32m                # does not decay with it. With no further data the statistics[m
[32m+[m[32m                # settle back to the penalty rather than to zero.[m
[32m+[m[32m                self._A[action] *= self.forget[m
[32m+[m[32m                self._b[action] *= self.forget[m
[32m+[m[32m                self._A[action] += (1.0 - self.forget) * self._penalty[m
[32m+[m
             self._A[action] += phi.T @ phi[m
             self._b[action] += phi.T @ y[m
             self._beta[action] = np.linalg.solve(self._A[action], self._b[action])[m
 [m
[32m+[m[32m    def refit(self, batch: list[Transition]) -> None:[m
[32m+[m[32m        """Discard the accumulated statistics and fit from scratch on `batch`.[m
[32m+[m
[32m+[m[32m        This is the alternative to the `forget` factor, and a better one. A[m
[32m+[m[32m        discount fades old experience uniformly with age, which cannot tell[m
[32m+[m[32m        "the navigation I learned in stage 1, still true" apart from "the value[m
[32m+[m[32m        estimate I made in stage 1, now wrong". Refitting from a replay buffer[m
[32m+[m[32m        keeps the experience and replaces only the estimates: the caller[m
[32m+[m[32m        recomputes every target against the current Q-function before calling[m
[32m+[m[32m        this, so nothing stale survives, while the transitions themselves are[m
[32m+[m[32m        still all there. That is fitted Q-iteration, and it is what PLAN.md[m
[32m+[m[32m        already specifies for Model B -- so using it here makes the two models[m
[32m+[m[32m        differ only in the function approximator.[m
[32m+[m[32m        """[m
[32m+[m[32m        dim = self.n_features + 1[m
[32m+[m[32m        for action in self.actions:[m
[32m+[m[32m            self._A[action] = self._penalty.copy()[m
[32m+[m[32m            self._b[action] = np.zeros(dim, dtype=np.float64)[m
[32m+[m[32m        self.update(batch)[m
[32m+[m
     def save(self, path: str) -> None:[m
         if os.path.isabs(path):[m
             raise ValueError("Model.save requires a relative path")[m
[36m@@ -104,6 +164,7 @@[m [mclass Model:[m
             n_features=self.n_features,[m
             n_step=self.n_step,[m
             gamma=self.gamma,[m
[32m+[m[32m            forget=self.forget,[m
             **arrays,[m
         )[m
 [m
[36m@@ -117,6 +178,8 @@[m [mclass Model:[m
         self.n_features = int(data['n_features'])[m
         self.n_step = int(data['n_step'])[m
         self.gamma = float(data['gamma'])[m
[32m+[m[32m        # Older files predate the forgetting factor; they mean forget = 1.0.[m
[32m+[m[32m        self.forget = float(data['forget']) if 'forget' in data else 1.0[m
 [m
         for action in self.actions:[m
             self._A[action] = data[f'A_{action}'][m
